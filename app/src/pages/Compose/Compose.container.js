import { connect } from 'react-redux'
import Compose from './Compose.component'

export const state = (state) => {
  return {}
}

export const dispatch = {}

export default connect(state, dispatch)(Compose)
