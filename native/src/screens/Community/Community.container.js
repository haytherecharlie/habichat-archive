import { connect } from 'react-redux'
import { path } from 'ramda'
import Community from './Community.component'

export const state = (state) => {
  return {}
}

export const dispatch = {}

export default connect(state, dispatch)(Community)
