import { connect } from 'react-redux'
import Chevron from './Chevron.component'

export const state = (state) => {
  return {}
}

export const dispatch = {}

export default connect(state, dispatch)(Chevron)
