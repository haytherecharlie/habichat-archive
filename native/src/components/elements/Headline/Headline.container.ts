import { connect } from 'react-redux'
import Headline from './Headline.component'

export const state = (state) => {
  return {}
}

export const dispatch = {}

export default connect(state, dispatch)(Headline)
