import { connect } from 'react-redux'
import { path } from 'ramda'
import VerifyInput from './VerifyInput.component'

export const state = (state) => {
  return {}
}

export const dispatch = {}

export default connect(state, dispatch)(VerifyInput)
