import { connect } from 'react-redux'
import { path } from 'ramda'
import EmailInput from './EmailInput.component'

export const state = (state) => {
  return {}
}

export const dispatch = {}

export default connect(state, dispatch)(EmailInput)
