import { connect } from 'react-redux'
import MessageList from './MessageList.component'

export const state = (state) => {
  return {}
}

export const dispatch = {}

export default connect(state, dispatch)(MessageList)
