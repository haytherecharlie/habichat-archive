import { connect } from 'react-redux'
import PinCodeInput from './PinCodeInput.component'

export const state = (state) => {
  return {}
}

export const dispatch = {}

export default connect(state, dispatch)(PinCodeInput)
