import { connect } from 'react-redux'
import TopNav from './TopNav.component'

export const state = (state) => {
  return {}
}

export const dispatch = {}

export default connect(state, dispatch)(TopNav)
