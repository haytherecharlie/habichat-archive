import { connect } from 'react-redux'
import ResidentList from './ResidentList.component'

export const state = (state) => {
  return {}
}

export const dispatch = {}

export default connect(state, dispatch)(ResidentList)
